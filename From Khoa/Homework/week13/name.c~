#include<stdio.h>

int main(){
  int i=0;
  char name[20];
  printf("Enter your name: ");
  gets(name);
  while(name[i]!=' ') i++;
  printf("Hello ");
  while(name[i]!='\0') printf("%c",name[i]);
  return 0;  
}
